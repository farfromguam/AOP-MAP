=== Council done-review — viewer_banded.html 9-patch frame rebuild ===
date: 2026-06-13 (session)
chair: Steward
goal: the banded-proof decorative frame must render as a true 9-patch (8 boundary-tracking
      border tiles) with lettering that holds size/location relative to the landmasses —
      not 4 viewport-pinned panels with floating text.
tier: core three (witness · warden · quartermaster) + mason — proof page only, no live
      viewer, no publish-zone data, no sprint boundary. Not full six.
scope reviewed (only these 3, fresh adversarial seats):
  website/viewer_banded.html · website/css/viewer_band.css · website/js/viewer_band.js
  (mvp/scripts/*.py|*.sh changes = pre-existing concurrent ★-star work, OUT of scope)
diff hash (gate formula, sha1(diff HEAD -- website mvp + status --porcelain -- website mvp)):
  00f36abb1a29605be2e585630b6c5c4771bdefde

SEAT: witness
VERDICT: clear
EVIDENCE: re-ran /tmp/verify_band_fix.py LIVE vs running server — boundary moved 55.0px,
  label offset drift 0.00px (off0=9.0 off1=9.0), all 8 tiles display!=none, 0 console errors.
  Reads real DOM (getBoundingClientRect + getComputedStyle) vs real window.AOPViewerBand.rawGaps(),
  not re-derived math. All 6 named PNGs exist in brain/output/; corner crops show mitred neat-line +
  diamond + edge-hugging text, map data masked (no spill). node --check passes. The 1 FAIL is a
  test-scenario assertion error (tall fit-to-region leaves top edge flush → place() correctly hides
  the top tile), bearing on no card claim.

SEAT: warden
VERDICT: clear
EVIDENCE: every hunk traces to the two directives (4 .band-edge strips → 8 boundary-tracking
  .band-tile objects; update() sizes tiles from the live 9-patch rect; setLabel() rides the true
  mid so text doesn't float). git diff --name-only touches no live-viewer file. No mutating git,
  no git add, no commit/attribution, no version bump. viewer_band/viewer_banded absent from
  sw.js/main.js → outside PWA precache → no #appVersion bump owed by this diff.

SEAT: quartermaster
VERDICT: clear
EVIDENCE: new update() reuses existing regionRect() (no duplicated bbox/projection helper);
  rawGaps()/gaps() untouched and still drive the leash. Old strip engine removed cleanly, not
  duplicated: band-edge=0, band-dot=0, setEdge=0, .v/.band-edge.sub=0 across all 3 files. No
  dangling DOM/class refs. New helpers (q/clamp/place/setLabel) are thin formatters. C1 (0 non-
  comment layerKey branches), C2 (one collectStarredDestinations), C6 (no new class/editor surface)
  all hold — untouched by this diff. node --check passes.

SEAT: mason
VERDICT: clear
EVIDENCE: 8-tile tiling partitions the viewport exactly — X = leftW+midW+rightW = W, Y = H, only
  the clamped center (the map) uncovered; no gaps/overlap; all 8 place() calls match their cells.
  place() w<=EPS||h<=EPS hide suppresses every tile (and its keyline) when the 9-patch fills the
  viewport. Label offset correct: span at translateX(-50%) + left=midX-Lx lands the center at
  absolute x=midX (true unclamped mid); rotate(180) on the left label is a post-centering glyph
  flip, anchor unaffected. No dead code (gaps→snapBack/peek button, rawGaps→enforceLeash still
  live). Idiomatic (var, closure helpers, comment density). No constraint/validator/row-dropping
  filter, no throwing strategy — place()'s hide is decorative-chrome suppression, not data filtering.

RESULT: CLEAR — every convened seat clear, no andon. Gate cleared; clearance marker written.
note: UNCOMMITTED — the commit + (if ever promoted to the live viewer) any version bump remain
      the user's git gate. This is proof-page work; viewer_band.{js,css} still drop into the core unchanged.

=== ADDENDUM — band snap-back tuning re-review (2026-06-13, same session) ===
change: removed the mid-pan Option-B leash (enforceLeash/axisCorrection/MAX_PULL/clamping/onMove +
        unused wpx/hpx). move/render/resize -> update() only; settle on RELEASE via snapBack().
        viewer_band.js only. Per user: "if we are mid pan it should not take it from us. only when we
        let go. a user can only scroll so far anyways."
re-review tier: Mason + Witness fresh on the new diff (Warden/Quartermaster trivially clear — pure
        deletion, same files; Mason's repo-wide grep confirms zero orphaned refs).

SEAT: mason     VERDICT: clear  — enforceLeash/axisCorrection/MAX_PULL/clamping/onMove/wpx/hpx fully
  removed, zero orphaned refs repo-wide; move/render/resize bind update directly; snapBack still gated
  by userDragged/snapping; rawGaps() correct after field drop; export updated; node --check passes;
  pure non-limiting removal.
SEAT: witness   VERDICT: clear  — re-ran /tmp/verify_band_snap.py LIVE: region g.t=0; held drag-down
  opened a 178.6px top gap (old cap 72 -> leash gone); mouse-up snapped to 0.0px; 0 errors; pixels in
  band_snap_1_middrag/2_released.png confirm peek+retract. Caveat: /tmp/verify_band_fix.py is 4/5 — the
  tall-viewport top-band ASSERTION is wrong (region-fit pins to height), layout code unaffected; not a
  regression.

RESULT: CLEAR for the band work. NOTE: clearance marker NOT written this round — the working tree now
  also holds a concurrent agent's unreviewed Sprint-13 slice-6 changes (viewer_core.js/viewer.html/
  viewer.css). The combined diff is that agent's gate to clear, not this one's. My 9-patch HTML+CSS were
  swept into the user's commit 2029871; only viewer_band.js (the snap tuning) remains uncommitted.

=== ADDENDUM 2 — corner mark swap (RW wordmark) re-review (2026-06-13, same session) ===
change: band corner decoration swapped from the orange CSS diamond to the Rock Warblers mark.
        Asset MOVED out of the brain: brain/prose/"qr mark.svg" -> website/assets/branding/rw-mark.svg.
        CSS: .band-tile.corner::after now mask: url("../assets/branding/rw-mark.svg") + background
        var(--band-rule), 22px, inset 6px. viewer_band.css only (+ asset move). Per user request.
re-review: Witness + Mason scoped to my files (concurrent viewer.css/sw.js/viewer.html are another
        agent's, excluded).
SEAT: witness  VERDICT: clear — curl rw-mark.svg = HTTP 200 (real SVG body); re-ran /tmp/verify_rw_mark.py
  live, ERRORS: 0; pixels in band_rwmark_{tr,bl,allsides}.png show the bird+RW mark at all 4 boundary
  corners in neat-line ink (not an empty/broken-mask box).
SEAT: mason     VERDICT: clear — path ../assets/branding/rw-mark.svg resolves from website/css/; file
  exists (5908B); both -webkit-mask + mask present; SVG mask-suitable (opaque-black paths, ink via
  background-color); no dead refs / no leftover .band-dot / diamond styling; pure presentational CSS,
  idiomatic to the --band-rule/--paper-band token style. (Noted + fixed: stale "corner diamonds"
  file-top comment -> "corner marks".)
RESULT: CLEAR for the band corner-mark change. Marker NOT written — combined working diff still holds
  concurrent slice-6/cleanup work (viewer.css/sw.js/viewer.html); that gate is the other agent's.

=== ADDENDUM 3 — band lettering scaled-to-the-map re-review (2026-06-13, same session) ===
change: lettering switched from fixed px to scaled — extentRatio() caches extent-per-font-px once,
        scaleLabel() sets font = fill*edge/ratio so each label spans a fixed fraction of its edge
        (FILL_TOP=0.58, FILL_SUB=0.70); uses clamped midW/midH (overflow-safe). viewer_band.js +
        a comment-only viewer_band.css change. Per user: "make it scale then so its fixed to the map."
re-review: Witness + Mason scoped to my 2 files (concurrent Slice-7 swap — index.html/viewer.html(D)/
        old_index.html/viewer.css/sw.js/data_editor_*(D) — excluded; not mine).
SEAT: witness  VERDICT: clear — live re-ran /tmp/verify_band_scale.py: 0 errors; observed DOM shows
  constant edge-fraction with changing font (TOP 58%→58% font 12.6→11.1px; RIGHT 68→70% font
  15.2→9.7px) = the scaling proof; PNGs band_scale_z{0_region,2_out}.png show no overflow, corner marks
  cleared; node --check passes.
SEAT: mason     VERDICT: clear — f*r0=fill*edge math sound (glyph + em letter-spacing scale linearly);
  extentRatio clears+restores inline font so _r0 is always the base ratio (re-entrancy-safe); r0<=0 +
  ext>0/base>0 guards prevent NaN/0 font; vertical tiles read offsetHeight (writing-mode axis); clamped
  midW/midH = overflow-safe; setLabel still positions (no orphan); idiomatic; no throw/filter.
RESULT: CLEAR for the band scaling change. Marker NOT written — combined working diff is dominated by the
  concurrent Slice-7 swap (not reviewed here, not mine to bless). Resolution: the swap's owner clears it,
  or the user commits the swap (their git gate); then the band diff alone can be cleanly cleared.

=== ADDENDUM 4 — corner-keyline fix + decoration variants + compare re-review (2026-06-13) ===
change: (1) removed the 4 corner-tile keyline borders (ran to viewport edge = "into infinity"); edge
        tiles already draw the 9-patch rectangle. (2) ?deco variants: rule (hairline flanks lettering),
        bracket (inner ⌐ at each corner mark), composable. (3) ?frame=out preview + new
        viewer_banded_compare.html (2x2). Files: viewer_band.css, viewer_banded.html, +compare page.
re-review: Witness + Mason scoped to my 3 files (concurrent Slice-7 swap excluded; not mine).
SEAT: witness  VERDICT: clear — live re-ran /tmp/verify_band_deco2.py: 0 errors, all 4 variants margins
  {90,90,120,120} topShown:true; pseudo reads confirm rule ::before ink #3a2c1a/260px & bracket ::before
  1px border, neither leaks into clean, both compose; PNGs show keyline as a clean rectangle around the
  9-patch (no infinity line), rule fills mark→text gap, bracket frames each corner, compare = real 2x2.
SEAT: mason     VERDICT: clear — corner-border removal leaves edge borders drawing the full rectangle (no
  gap), no stray refs; variants scoped via [data-deco~=] so clean (no attr) is untouched, ~= composes
  "rule bracket"; param IIFE bounded (clearInterval at >40 ticks), guards before acting (no throw),
  replace(/\+/g) harmless-redundant; idiomatic; pure presentation, no limiting code.
RESULT: CLEAR for the band deco work. Marker NOT written — combined working diff still holds the concurrent
  Slice-7 swap (and the advisory sw.js/#appVersion note belongs to that, not the proof page). Open product
  decision: user to pick a variant (rec: rule).

=== ADDENDUM 5 — band design SETTLED (rule default, thinner, shadows removed) (2026-06-13) ===
user: "go with the hairline rule make it thinner ... it has the borders as last discussed" (+ earlier
      reported "dropshadows going to infinity on some corners").
change: (1) removed the box-shadow from all 4 .band-tile.edge rules — each tile spans its whole margin so
        its shadow bled along the perpendicular edges to the viewport corner (same "infinity" class as the
        old corner border); frame is now a flat printed neat-line. (2) rule hairline 1.4px -> 0.75px (both
        orientations). (3) #bandFrame data-deco="rule" is the markup DEFAULT (no-param load shows rule);
        ?deco still overrides. Files: viewer_band.css, viewer_banded.html.
SEAT: witness  VERDICT: clear — live re-ran /tmp/verify_band_final.py + DOM read of all 8 tiles: data-deco
  =rule, every tile box-shadow=none, rule 0.75px ink, margins {90,90,120,120}, 0 errors; band_final{,_tl,_tr}.png
  show thin rule + keyline + corner marks, clean margins, NO shadow bleed toward any viewport corner.
SEAT: mason     VERDICT: clear — box-shadow gone (only unrelated #bandProofHud keeps one); keyline 1.5px &
  bracket 1.2px untouched; 0.75px on both rule orientations only; data-deco="rule" matched by [data-deco~=
  "rule"], param script overrides when present, unknown/missing token falls back to clean baseline (no
  throw); no dead code, idiomatic. NOTE (user/Warden call): uncommitted diff also carries the bracket
  variant + compare page (coherent proof scaffolding) — decide what ships.
RESULT: CLEAR — band aesthetic SETTLED. Marker still NOT written (combined working diff holds the concurrent
  Slice-7 swap). To clear the gate: commit the swap (user's git gate), then the band diff alone clears.
