### SCOPED DIFF — this task only (mockup cleanup + contour styling mockup) ###

## website/ status (D = my deletions, ?? = my new file) ##
 D website/calendar_placeholder_v2_spinner.html
 M website/data/gold_aop_trail_network.geojson
 D website/icon_master.html
 M website/index.html
 D website/leftrail_compare_v2.html
 D website/leftrail_current.html
 D website/leftrail_eventflow_focus.html
 D website/leftrail_eventflow_ribbon.html
 D website/leftrail_eventflow_timeline.html
 D website/leftrail_gates_dash.html
 D website/leftrail_gates_horizon.html
 D website/leftrail_gates_tag.html
 D website/leftrail_motion.html
 D website/leftrail_tabs_blaze.html
 D website/leftrail_tabs_manilla.html
 D website/leftrail_tabs_ruled.html
 D website/load_animations.html
 D website/old_index.html
 M website/sw.js
?? website/contour_styling_mockup.html

## index.html + sw.js diff (MINE = comment hunks; IGNORE the v94->v95 bump hunks = other session) ##
diff --git a/website/index.html b/website/index.html
index 66aead2..1fa1b3f 100644
--- a/website/index.html
+++ b/website/index.html
@@ -7,7 +7,7 @@
   <!-- AOP viewer — the front-end read view (Sprint 13 viewer extraction). Rebuilt
        clean ALONGSIDE the old all-in-one page, then promoted to index.html at the
        swap (slice 7, brain/tasks/13_viewer_extraction/viewer_swap.md). The old
-       tangled page is parked at old_index.html; the editor lives in panel.js + the
+       all-in-one page has been retired; the editor lives in panel.js + the
        standalone field tools (schedule_editor.html, data_editor.html) + QGIS.
        Carries the map, the pill-bar (zoom / preset / 3D), the left-rail drawer
        (Search / Hot / Calendar→Events/POI/About), the blue Locate FAB, and Install.
@@ -174,7 +174,7 @@
                     <span class="calendar-countdown-value" id="calendarCountdownValue">--</span>
                   </div>
                   <ol class="calendar-days" id="calendarDays">
-                    <!-- Spinner loading row (calendar_placeholder_v2_spinner.html, "V2").
+                    <!-- Spinner loading row.
                          viewer_core.js replaces calendarDays.innerHTML with the real
                          session rows the moment the event-schedule JSON resolves, so
                          this shows only while the schedule is actually loading. -->
@@ -195,8 +195,8 @@
                      The three card handles drive one shared --lr-card-body-height var
                      on .lr-content-col (viewer_core.js initLrCardResize), so the
                      Events / POI / About bodies stay the same height across tab
-                     switches. Restored from old_index.html (the per-card resize
-                     handles deferred at the viewer swap). -->
+                     switches. (The per-card resize handles were deferred at the
+                     viewer swap, then restored.) -->
                 <div id="calendarResizeHandle" class="lr-resize-handle" role="separator" aria-label="Resize calendar" aria-orientation="horizontal" aria-controls="calendarBody" aria-valuenow="240" tabindex="0"></div>
               </div>
               <div id="poiTabPanel" class="left-tab-panel" role="tabpanel" aria-labelledby="leftTabPoi" hidden>
@@ -231,7 +231,7 @@
          viewer_core.js folds it beside the collapsed bottom-left ⓘ ("ⓘ vNN",
          hidden when expanded) and also reads it into the expanded attribution body
          as "Made by Rock Warblers · vNN". Starts hidden; JS unhides on fold. -->
-    <span id="appVersion" hidden>v94</span>
+    <span id="appVersion" hidden>v95</span>
 
     <!-- iOS Safari Add-to-Home-Screen steps (toggled by the install button on iOS,
          where there is no programmatic install prompt). -->
diff --git a/website/sw.js b/website/sw.js
index c52b6c7..1afbe7d 100644
--- a/website/sw.js
+++ b/website/sw.js
@@ -32,7 +32,7 @@
 //   3. reconcile DATA_ASSETS below with `ls website/data/`
 // Shell HTML + copy JSON self-heal (stale-while-revalidate), so a missed bump is
 // less dangerous than before — but bulky GeoJSON only refreshes on a bump.
-const VERSION = 'v94'; // keep in sync with #appVersion in index.html
+const VERSION = 'v95'; // keep in sync with #appVersion in index.html
 const SHELL_CACHE = `aop-shell-${VERSION}`;
 const DATA_CACHE = `aop-data-${VERSION}`;
 const TILE_CACHE = 'aop-tiles'; // unversioned on purpose — see header note
@@ -68,9 +68,9 @@ const SHELL_ASSETS = [
   // viewer is offline-first; the band fetches the corner mark below at runtime.
   './js/viewer_band.js',
   './assets/branding/rw-mark.svg',
-  // The old all-in-one page is parked at old_index.html and still uses these
-  // (main.js + the embedded panel); the standalone field editors load them too.
-  // Kept in the precache shell so those pages also work offline.
+  // The standalone field editors (data_editor.html / schedule_editor.html /
+  // right_panel.html) load main.js + the embedded panel, so they are kept in the
+  // precache shell so those pages also work offline.
   './js/main.js',
   // Right-panel swap (Stage 1): the embedded one-model panel + its scoped styles.
   './css/panel-embed.css',
