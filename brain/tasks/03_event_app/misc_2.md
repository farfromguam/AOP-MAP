
make a date time slider in the right side so that we can adjust the virtual time to test the stylis live for the 
I want to be able to advance times by days or hours. so maybe two inputs???

make a third hot item ??? -- landmarks

make a reset to new user experience button in bottom right. basically clear any button presses and default to first view.
make a users settings persist across reloads. if they were on the topo view it should go back there... If search was open that should be re-opened. if a calendar event was selected... maybe ??? as much as we reasonably can.
we want them to be able to pull this out of their pockets many times and not loose context. think usrer folding map so section is visible on top and stuffing it into bag. same thing.

-----

## Routed 2026-05-26

- Virtual date/time slider, reset-to-new-user, pocket-map persistence, and the
  landmarks-hot decision now live on `viewer_session_state_test_clock.md`.
