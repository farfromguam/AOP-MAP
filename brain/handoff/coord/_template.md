---
session: EXAMPLE — rename to your slug (e.g. opus-ui, or the card you're on)
status: template        # working | blocked | done
claim: EXAMPLE — the files or card you're touching (your soft lock)
started: YYYY-MM-DDTHH:MM
---

Copy this file to `<session-slug>.md` when you start live work alongside another session.
You own your copy; nobody else writes it. Read the other files here before you claim.
On finish: drain the durable note to `session_context.md`, then set `status: done` with a
`next:` line below (or delete this file). See `_protocol.md`.

- HH:MM claimed <area / card>, starting <what>
- HH:MM <progress note>
- DONE → next: <what the next session should pick up>
