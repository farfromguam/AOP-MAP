# Session Handoff: MVP map work

Date: 20260522

This is the live session-handoff pointer for the AOP MVP map work. It is short by design — the brain holds the durable record.

## Archive convention

Each session pruned out of here lands at `session_context_<YYYYMMDD>.md`:

- `session_context_202605201517.md` — original handoff shape (the template).
- `session_context_20260522.md` — full work log from the 2026‑05‑20 → 2026‑05‑22 sessions (lidar contours, water, search, cemeteries, land cover on lidar CHM, NAIP tracing, presets, panel collapse, activity hotspots, event schedule).

Read the archive only if you need to retrace why something was built. The durable record for each feature lives in its `tasks/01_mvp/<feature>.md` build card, `research/viewer.md`, `search_map.md`, or `spinup/mvp_runbook.md`.

## Where we are

The static viewer at `website/index.html` is the live work surface. It carries ~25 toggleable layers, a POI / line-trace editor, a JSON-driven event schedule sidebar, and Region / Park / Pavilion zoom presets bounded to the 9-patch. The Docker/PostGIS MVP database is wired but only holds demo data and one source-backed AOP parcel boundary; everything else in the viewer reads from `website/data/*.geojson` exports and public imagery/terrain tiles.

Last shipped: schedule clock-times (2026-05-24). `aop_event_schedule.json` now carries per-session `start_local` (`HH:MM` 24h local) alongside the existing `time_label` day-part; calendar rows + session popups display the composed `"5:00 PM · Evening"`. Two helpers added next to `eventScheduleToGeojson` (`formatEventStartLocal`, `composeEventWindowLabel`). This unblocks the Sprint 02 Bucket A3 left-hot-button "imminent ≤ 30 min" rule — no remaining schema blocker. `playwright_verify_event_schedule.py` re-run end-to-end: PASS, 0 console errors (popup line confirmed reading `Time: 6:30 PM · Evening`). Durable records: `tasks/01_mvp/event_schedule_layer.md` (schema), `tasks/02_edit/viewer_chrome_polish.md` B4 (follow-up note), `tasks/02_edit/left_hot_button.md` (unblocked).

Last shipped: Sprint 02 Bucket H code-health Pass 3 (2026-05-24). `tasks/01_mvp/code_health_pass.md` carries the durable record. Viewer-side: 7 localStorage keys hoisted as a single block, `readJsonStore`/`writeJsonStore`/`mergeStoreSlice` helpers replace 6+ inline try/catch read-merge-write blocks, 3 `load*Store`/`save*Store` pairs collapsed to one-liners, the dead-comment block in `applySectionPayload` trimmed, CSS palette tokens (`--brown-ink`, `--brown-mid`, `--brown-dark`, `--brown-soft`, `--cream-hover`, `--rust`) hoisted with 49 sites swapped via line-range sed (JS hex strings untouched). Verifier-side: `mvp/scripts/playwright_base.py` is now the single home for `WEBSITE_URL`, `set_toggle` (DOM-driven form that works through collapsed sections), `layer_visibility`, `rendered_count`, and a new `click_in_section` helper that auto-expands a collapsed `.panel-section` before clicking; 18 verifiers migrated, 2 (landcover, poi_editor) additionally patched to use `click_in_section` for raw-`.click()` sites that had been timing out on default-collapsed sections since Sprint 02. Verification: viewer JS parses clean (`node --check`), 9 verifiers re-run end-to-end against the refactored viewer — 8 fully clean (water 29/0, satellite, event_schedule, buildings, terrain 23/0, visitor_context 24/0, landcover 37/0, poi_editor 50/0); feature_list returns 112 PASS / 1 FAIL where the lone failure is a pre-existing stale assertion (`derived-layers payload carries visitor-context-fill paint` expects the `showVisitorContext` toggle in derived-layers, but it has been in `publishable` for several commits — reproduces on master). Remaining 9 verifiers parse clean and use the shared helpers but were not re-run; next session's verifier sweep picks them up.

Also written this session, no build started: Sprint 02 Bucket A3 left-hot-button spec at `tasks/02_edit/left_hot_button.md`. The button has three states — **hot-now** (a session is live or starts within 30 min), **coming-up** (no imminent session but the next one is on the schedule any day forward — Friday should already light up for a Saturday evening session and show the countdown), and **heatmap-fallback** (schedule empty or past → surfaces the activity-hotspots layer). Demo posture: today is Saturday for development (so the default test path is "live session today" plus the upcoming-tomorrow case). Build cost ~half-day if the schedule resolver + `gotoEventSession` path stay in their current shape.

Previously shipped: Sprint 02 Bucket A2 default-layer policy + Bucket E buildings default-on (2026-05-23). `tasks/02_edit/views_and_defaults.md` carries the durable record. Fresh-load + Park preset now draw streams/waterbodies and the 4 in-park FEMA building footprints alongside the existing land cover / roads / trails / boundary / trailheads / visitor-context payload; Topo gains the buildings on top of its existing hillshade/contours/water/springs; Trace is unchanged. Springs stays topo-only. Code surface: three checkbox flips in `website/index.html` (fresh HTML `#showWater` + `#showBuildings`, `BUILT_IN_PRESETS.park.toggles` `showWater`/`showBuildings`, `BUILT_IN_PRESETS.topo.toggles` `showBuildings`). The outside-park 198 footprints stay collapsed under the feature-list panel's default-off bulk toggle, so the on-by-default layer does not flood the Park view. Verifiers `playwright_verify_water.py` (PASS, 18/18 + 0 console errors), `playwright_verify_buildings.py` (PASS, 20/20 + 0 console errors), and `playwright_verify_presets.py` (A2 assertions PASS) extended. Both water + buildings verifiers also picked up the `.checked` + change-event `set_toggle` pattern from `playwright_verify_event_schedule.py` so a checkbox inside a collapsed `panel-section` no longer times out. Pre-existing presets-verifier failures (the "land-cover outputs live under Derived layers" assertion is stale — `showVisitorContext` now lives in `publishable`; the trails inline-tuning step times out clicking the collapsed Publishable row) reproduce on master and are unrelated. Bucket E closes through this card. Sprint 02 Bucket A1 (`personas.md`) is also written as a reference doc that frames future view/default decisions.

Previously shipped: Sprint 02 Bucket D named-feature tagging (2026-05-23). `tasks/02_edit/named_feature_tagging.md` carries the durable record. Per-feature `#tag` input on Buildings + Drawn POIs rows of the shared feature list panel; `aop_feature_tags_v1` localStorage maps `{ layerKey: { featureId: "#tag" } }` with an inverted `tagToFeature` map rebuilt on every layer register. `resolveEventLocation` now falls back to that map when `locations[#tag].coordinates` is absent. `aop_event_schedule.json`'s `#pavilion` entry no longer carries `coordinates` — a one-time seed (`aop_feature_tags_seeded_v1`) binds `#pavilion` to the 1010 Ellis Cove Rd building on first load, and the resolver picks up the building's centroid. Live re-bind of a tag re-runs the schedule resolver, updates the `event-schedule` MapLibre source, re-renders the calendar, and refreshes the event-schedule slice of `searchIndex` so `#pavilion` stays searchable as the anchor moves. Tag store rides the existing per-section + bulk Export/Import. Verifier `playwright_verify_event_schedule.py` extended with 14 Bucket D assertions (seed, lookup, live re-bind, per-row tag input rendering on all 202 buildings, camera flight to resolved coords, search hit) -- all PASS, 0 console errors. Pre-existing `set_toggle` brittleness fixed in the same pass: it now drives `.checked` + change event instead of a UI click, so a checkbox inside a collapsed `.panel-section` no longer times out.

Previously shipped: Sprint 02 Bucket C search-tags (2026-05-23). `tasks/02_edit/search_tags.md` carries the durable record. `indexFeatures` gained an optional `aliasesFor(props)` parameter; event-schedule anchors now pass their `#tag` through it. `buildSearchGroups` unions aliases per group, and `searchGroupMatchesQuery` honors a leading `#` as a tag-only switch so a `#pavilion` query returns the pavilion anchor instead of every session that staged there. Sessions intentionally stay indexed by title only -- the dropdown would otherwise drown when a popular tag is typed. Verifier `playwright_verify_search.py` extended with 11 tag-search assertions, all PASS, zero console errors. The two pre-existing FAILs ("multi-segment trail collapses to one result") reproduce on master before this diff and are unrelated -- the event_anchor "Trailhead - Saturday Afternoon segment 2" shares a substring with the observed trail.

Previously shipped: Sprint 02 Bucket B chrome-polish wave (2026-05-23). All five items in `tasks/02_edit/viewer_chrome_polish.md` shipped in one session:

- B1 — `visibleMapPadding` rewritten on a new `visibleMapRect()` helper; `panPopupIntoView` nudges the camera after `moveend` so the calendar-link popup lands inside the unoccluded slice; popup `maxWidth` clamped to slice width [200, 280] for cramped layouts.
- B2 — inline SVG magnifier in `.search` shell, `pointer-events:none`, padding-left adjustment on `#searchInput`.
- B3 — panel now docks bottom-right (anchor flipped from `top: 12px` → `bottom: 12px`), `box-sizing: border-box` so `max-height` includes padding, mobile reserve bumped to `calc(100vh - 280px)` and `width: auto` so the bottom-anchored panel clears the auto-collapsed calendar bar above and the message bar below.
- B4 — calendar auto-collapses on ≤760 px first load, user expand/collapse persists in `aop_calendar_collapsed_v1`. Removed the stale unconditional `setCalendarCollapsed(false)` that was clobbering the new init.
- B5 — trace preset gives `roads-labels`, `osm-named-labels`, `activity-hotspots-labels`, `visitor-context-labels` a dark `#15110d` halo so cream text reads against the busy SFWDA paper map; park/topo presets explicitly reset to cream `#f7f1e2` halo so `trace→park` reverts cleanly.

Verifiers extended: `playwright_verify_event_schedule.py` (B1/B2/B4 — narrow popup, magnifier geometry, calendar auto-collapse + time-label per row), `playwright_verify_presets.py` (B3 — bottom-dock geometry on wide + mobile, B5 — trace halo + park revert). Both add a "Failed to fetch" filter for navigation-aborted MapLibre fetches triggered by viewport-resize reloads.

Card: `brain/tasks/02_edit/viewer_chrome_polish.md` (all five items marked shipped, includes implementation + acceptance per item). Triage `brain/tasks/02_edit/_readme.md` reflects the new card. The two search-verifier FAILs ("multi-segment trail collapses to one result") are pre-existing on master, unrelated.

Bucket A decisions (views/personas, always-on layer set, left-side hot button) still owed. User confirmed on 2026-05-23 to keep cutting through executable work before surfacing -- Buckets C + D now shipped on that direction. Remaining ready-to-pick: E (buildings default-on, gated on A2), F (branding placement, gated on AOP permission ask). H (CSS/code Pass 3) folds into `../01_mvp/code_health_pass.md` once the editor churn settles. D's curation half (cabins, campsites, trail-feature POIs the user draws + tags through the new `#tag` input; SFWDA trail-name transcription) is user-led; the editor surface is now in place for it.

Previous shipped: feature list panel — POI consumer + drag-to-move primitive + visitor-context drag + map→panel reveal + per-section/bulk Export-Import (2026-05-23). The shared find+move primitive has four consumers (POIs + buildings + cemeteries + visitor-context callouts) and a fifth panel-side hook (revealFeatureInPanel) closes the map-click → panel-row loop. Each panel section now has its own ↑/↓ Export/Import on its header (schema `aop-section-state-v1`); the panel footer has Export-all / Import-all (`aop-viewer-preset-settings-v2`). Snapshot Preset + the in-drawer Export Settings buttons retired. Build card `tasks/02_edit/poi_editor_v2.md`, verifier `mvp/scripts/playwright_verify_feature_list.py`. Bucket B's "region circle callouts need to be positionable" closes through this work. Sprint 02 branding asset drop is staged at `tasks/02_edit/assets/branding/raw/` awaiting placement + AOP permission decisions (Bucket F).

Current synthetic activity work: `mvp/scripts/simulate_saturday_activity.py` generates the pavilion-start Saturday model; `mvp/scripts/build_activity_hotspots.py` extracts both raw GPX and synthetic hotspots. The current synthetic layer follows nearby OSM `highway=track` / `service` linework, then filters hotspot output to stopped+slow dwell/crawl cells (`rank_by=stop_slow`) so it no longer renders as a dotted route. Pickup docs: `tasks/01_mvp/activity_hotspots.md`, `research/viewer.md`, and `mvp/scripts/README.md`. Likely next tweak: add a region/AOI argument to the simulator or hotspot builder so a user can force activity into a smaller polygon/bbox and produce more crossover/localized hotspots.

Previous shipped: tag-driven event schedule sidebar (2026-05-22). Build card `tasks/01_mvp/event_schedule_layer.md`.

Last code-health pass: Pass 2 closed (2026-05-22). Three sources-of-truth for the viewer toggle set collapsed to one (`LAYER_TOGGLES` is now authoritative). Playwright URL drift across 7 verifiers fixed; `WEBSITE_URL` env override now uniform. Pulse-animation constants named, `sliderPercent` helper extracted, two Python `except BaseException` blocks scoped, a NULL-edge-case in `import_gpx_track.sql` closed with `IS NOT DISTINCT FROM`. The three Pass 1 user-call items resolved (`import_geojson.sh` wired up, `secondary`/`ramp` road classes styled defensively, repo litter removed: `mvp/db-data-broken-*`, `mvp/website/`, 100 PNGs in `brain/output/`). Card: `tasks/01_mvp/code_health_pass.md`.

## Live preview ports

- Human/manual preview: `cd website && python3 -m http.server 8000` → `http://localhost:8000/`
- Playwright verifiers: `cd website && python3 -m http.server 8001` → `http://localhost:8001/`. If 8001 is occupied, clean up the stale Playwright viewer and reload 8001 instead of starting a new numbered localhost. Do not fall back to 8000 for Playwright.

## What to pick up next

The MVP backlog is the source of truth — see `tasks/01_mvp/_readme.md`. Unstarted items, in roughly the order that unblocks the V1 publishable map:

1. Item 9 — replace demo `trail_centerlines` / `trailheads` placeholders with real source-backed AOP data, then re-export `publish.geojson`. This is the gap between "lots of context layers" and "actual AOP map."
2. Item 3 — connect QGIS to `localhost:55432` and inspect `raw.arcgis_feature_captures`, `core.parcels`, `core.park_boundaries`, `source_register.feature_sources`.
3. Item 8 — reconcile the 600+ acre official AOP claim against the imported `110 008.00` + `093 030.01` envelope (currently ~592 calc acres / ~573 deed acres).
4. ~~Item 11 — code-health remediation pass per `code_health_pass.md`.~~ Pass 2 closed (2026-05-22). Cosmetic deferrals carried forward for a Pass 3 when adjacent code is being touched.
5. Item 10 — swap the AWS Terrarium DEM for AOP-specific tiles from the USGS 3DEP 1 m DEM. Half-day toolchain + ~500 MB download; only worth it once the 10 m terrarium look has earned its keep.

## Loose ends not yet on a card

- SFWDA paper-map alignment: the 4-corner image-warp is inspection-grade. Decision still owed on whether true georeferencing (GCPs + affine/projective in GDAL/QGIS) is required before any SFWDA trail centerline can be promoted to `core.trail_centerlines`. Captured in `tasks/01_mvp/community_trails_import.md`.
- `source_register.sources` rows still owed before any raw-zone context (OSM tracks/landmarks, NHD water, USGenWeb Ellis burial roster, FEMA building footprints) is promoted to a `publish.*` view. Rules: `northstar/source_register.md`.

## Sidecar artifact kept in this folder

- `event_schedule_context_20260522.json` — actively referenced by `tasks/01_mvp/event_schedule_layer.md` and `research/viewer.md` as the sister-event research input. Leave in place until the event schedule card is closed.

## Session note

This file is handoff context, not a durable policy document. Keep it short. When a session ends, prune this file back to a pointer and archive the changelog to `session_context_<YYYYMMDD>.md`. Do not append session-by-session update blocks here — they belong in build cards, `research/viewer.md`, `search_map.md`, or `spinup/mvp_runbook.md`.
