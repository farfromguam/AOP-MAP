# CLAUDE

TL;DR:
- Read `AGENTS.md` in this same directory.
- Then follow its instructions to enter the brain.
- Do not duplicate orientation content here.

#claude #pointer #agents

-----

This repo uses a single shared init file for all agents: `AGENTS.md`.

That file tells you what to read, in what order, before acting. The truth lives in `brain/`. This file and `AGENTS.md` only help you get there.

Go read `AGENTS.md` now.
